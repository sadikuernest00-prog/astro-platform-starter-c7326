---
const featuresBorrower = [
  "Reach real people willing to lend, not just banks.",
  "Borrow in normal money or crypto, depending on what you and the lender agree.",
  "Use your own wallets to move crypto directly.",
  "Explain your situation in your own words.",
  "Negotiate repayment terms directly with lenders.",
  "Whenever possible, we match you with manually verified lenders.",
];

const featuresLender = [
  "See real people and projects, not just numbers.",
  "Lend using bank transfer, crypto, or both – you decide.",
  "Use your own wallets or exchange accounts to send and receive crypto.",
  "Set your own interest rate and conditions.",
  "Chat with borrowers before you commit.",
  "Apply for a Verified lender badge after a quick manual review.",
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Amicbridge – Borrow or Lend Money & Crypto</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      :root {
        color-scheme: dark;
        --bg: #020617;
        --card: #0f172a;
        --accent: #22c55e;
        --text: #e5e7eb;
        --muted: #9ca3af;
        --border: #1f2937;
      }
      body {
        margin: 0;
        background: radial-gradient(circle at top, #0f172a 0, #020617 55%);
        color: var(--text);
        font-family: system-ui, sans-serif;
      }
      .shell {
        max-width: 1000px;
        margin: 0 auto;
        padding: 2rem 1.5rem 3rem;
      }
      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      nav a {
        margin-left: 1.2rem;
        color: var(--muted);
        text-decoration: none;
      }
      nav a.button {
        background: var(--accent);
        padding: 0.45rem 0.95rem;
        border-radius: 999px;
        color: #022c22;
        font-weight: 600;
      }
      h1 {
        font-size: 3rem;
        margin-bottom: 1rem;
        line-height: 1.1;
      }
      h1 span {
        color: var(--accent);
      }
      .hero-btns button {
        padding: 0.7rem 1.4rem;
        border-radius: 999px;
        font-size: 1rem;
        border: 1px solid var(--border);
        cursor: pointer;
        background: transparent;
        color: var(--text);
      }
      .hero-btns .primary {
        background: var(--accent);
        color: #022c22;
        border: none;
      }
      .form-card {
        background: rgba(15, 23, 42, 0.8);
        padding: 1.5rem;
        margin-top: 2rem;
        border-radius: 1rem;
        border: 1px solid rgba(255,255,255,0.08);
      }
      input, textarea, select {
        width: 100%;
        margin-top: .5rem;
        margin-bottom: 1rem;
        padding: .7rem;
        border-radius: .6rem;
        background: #0b1324;
        border: 1px solid #1e293b;
        color: white;
        font-size: .95rem;
      }
      textarea {
        min-height: 130px;
      }
      .submit-btn {
        background: var(--accent);
        color: #022c22;
        padding: .8rem 1.6rem;
        border-radius: 999px;
        cursor: pointer;
        font-weight: 600;
        border: none;
      }
      footer {
        margin-top: 3rem;
        padding-top: 1.5rem;
        border-top: 1px solid rgba(255,255,255,0.1);
        color: var(--muted);
      }
    </style>

    <script>
      function showForm(type) {
        document.getElementById("borrow-form").style.display = type === "borrow" ? "block" : "none";
        document.getElementById("lend-form").style.display = type === "lend" ? "block" : "none";
        window.location.hash = type === "borrow" ? "#borrow-form" : "#lend-form";
      }
    </script>
  </head>

  <body>
    <div class="shell">

      <header>
        <h2>Amicbridge</h2>
        <nav>
          <a href="#how">How it works</a>
          <a href="#contact">Contact</a>
          <a href="#borrow-form" class="button">Get started</a>
        </nav>
      </header>

      <section class="hero">
        <h1>
          Borrow from people.<br />
          <span>Lend to people.</span><br />
          One simple bridge.
        </h1>
        <p>Amicbridge connects borrowers and lenders using normal money or crypto. You set the terms — we help you match.</p>

        <div class="hero-btns">
          <button class="primary" onclick="showForm('borrow')">I want to borrow</button>
          <button onclick="showForm('lend')">I want to lend</button>
        </div>
      </section>

      <!-- Borrow Form -->
      <section id="borrow-form" class="form-card" style="display:none;">
        <h2>Borrow money or crypto</h2>
        <form name="borrow" method="POST" netlify>
          <input type="text" name="name" placeholder="Your name" required />
          <input type="email" name="email" placeholder="Email" required />
          <input type="text" name="phone" placeholder="Phone number" required />

          <select name="currency">
            <option value="normal">Normal money (bank)</option>
            <option value="crypto">Crypto</option>
          </select>

          <input type="number" name="amount" placeholder="Amount you want to borrow" required />

          <textarea name="message" placeholder="Tell us why you need this loan"></textarea>

          <button class="submit-btn">Send request</button>
        </form>
      </section>

      <!-- Lend Form -->
      <section id="lend-form" class="form-card" style="display:none;">
        <h2>Lend money or crypto</h2>
        <form name="lend" method="POST" netlify>
          <input type="text" name="name" placeholder="Your name" required />
          <input type="email" name="email" placeholder="Email" required />
          <input type="text" name="phone" placeholder="Phone number" required />

          <select name="currency">
            <option value="normal">Normal money (bank)</option>
            <option value="crypto">Crypto</option>
          </select>

          <input type="number" name="amount" placeholder="Amount you want to lend" required />

          <textarea name="message" placeholder="Tell borrowers what kind of loan you offer"></textarea>

          <button class="submit-btn">Publish lending offer</button>
        </form>
      </section>

      <!-- Features Section -->
      <section id="how" class="form-card">
        <h2>How it works</h2>

        <h3>For borrowers</h3>
        <ul>
          {featuresBorrower.map(item => <li>{item}</li>)}
        </ul>

        <h3>For lenders</h3>
        <ul>
          {featuresLender.map(item => <li>{item}</li>)}
        </ul>
      </section>

      <!-- Contact -->
      <section id="contact" class="form-card">
        <h2>Contact</h2>
        <p><strong>CEO:</strong> Robert</p>
        <p><strong>Address:</strong> Oslo, Norway</p>
        <p><strong>Phone:</strong> 929 28 373</p>
        <p><strong>Email:</strong> robertamicolsen@gmail.com</p>
      </section>

      <footer>
        <p><strong>Important:</strong> Amicbridge is a connection platform. We do not provide loans, hold money, or guarantee repayments. Always verify who you are dealing with.</p>
      </footer>

    </div>
  </body>
</html>
