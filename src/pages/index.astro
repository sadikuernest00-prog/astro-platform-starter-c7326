---
const featuresBorrower = [
  "Reach real people willing to lend, not just banks.",
  "Explain your situation in your own words.",
  "Negotiate repayment terms directly with lenders.",
];

const featuresLender = [
  "See real people and projects, not just numbers.",
  "Set your own interest rate and conditions.",
  "Chat with borrowers before you commit.",
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Amicbridge – Borrow and Lend Directly</title>
    <meta
      name="description"
      content="Amicbridge connects people who want to borrow money with people who want to lend – directly, person to person."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      :root {
        color-scheme: dark;
        --bg: #020617;
        --card: #020617;
        --accent: #22c55e;
        --accent-soft: #064e3b;
        --text: #e5e7eb;
        --muted: #9ca3af;
        --border: #1f2933;
      }
      body {
        margin: 0;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text",
          sans-serif;
        background: radial-gradient(circle at top, #0f172a 0, #020617 55%);
        color: var(--text);
      }
      .shell {
        max-width: 1000px;
        margin: 0 auto;
        padding: 2.5rem 1.5rem 4rem;
      }
      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
      }
      .logo {
        display: flex;
        align-items: center;
        gap: 0.6rem;
        font-weight: 600;
        letter-spacing: 0.04em;
      }
      .logo-mark {
        width: 26px;
        height: 26px;
        border-radius: 999px;
        background: radial-gradient(circle at 30% 20%, #4ade80, #16a34a);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.9rem;
        color: #022c22;
      }
      nav a {
        font-size: 0.9rem;
        color: var(--muted);
        text-decoration: none;
        margin-left: 1.2rem;
      }
      nav a.button {
        color: #022c22;
        background: var(--accent);
        padding: 0.45rem 0.95rem;
        border-radius: 999px;
        font-weight: 500;
      }
      .hero {
        display: grid;
        gap: 2rem;
        margin-top: 1rem;
      }
      @media (min-width: 800px) {
        .hero {
          grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr);
          align-items: center;
        }
      }
      h1 {
        font-size: clamp(2.3rem, 3vw + 1.2rem, 3.1rem);
        line-height: 1.05;
        margin-bottom: 1rem;
      }
      h1 span {
        color: var(--accent);
      }
      .hero-text {
        font-size: 1rem;
        color: var(--muted);
        max-width: 32rem;
        margin-bottom: 1.5rem;
      }
      .cta-row {
        display: flex;
        flex-wrap: wrap;
        gap: 0.8rem;
        margin-bottom: 1.2rem;
      }
      .btn {
        border-radius: 999px;
        padding: 0.6rem 1.4rem;
        border: 1px solid transparent;
        font-size: 0.98rem;
        font-weight: 500;
        cursor: pointer;
        background: transparent;
        color: var(--text);
      }
      .btn-primary {
        background: var(--accent);
        color: #022c22;
      }
      .btn-outline {
        border-color: var(--border);
      }
      .tiny-note {
        font-size: 0.78rem;
        color: var(--muted);
      }
      .card {
        background: rgba(15, 23, 42, 0.84);
        border-radius: 1.2rem;
        padding: 1.4rem 1.5rem;
        border: 1px solid rgba(148, 163, 184, 0.18);
        box-shadow: 0 18px 45px rgba(15, 23, 42, 0.9);
      }
      .pill {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        font-size: 0.75rem;
        padding: 0.2rem 0.7rem;
        border-radius: 999px;
        background: rgba(15, 118, 110, 0.18);
        color: #6ee7b7;
        margin-bottom: 0.7rem;
      }
      .pill-dot {
        width: 6px;
        height: 6px;
        border-radius: 999px;
        background: #4ade80;
      }
      .inline-label {
        font-size: 0.78rem;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: var(--muted);
        margin-bottom: 0.2rem;
      }
      .input-row {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 0.7rem;
        margin-bottom: 0.7rem;
      }
      .fake-input {
        padding: 0.65rem 0.8rem;
        border-radius: 0.7rem;
        border: 1px solid rgba(148, 163, 184, 0.5);
        font-size: 0.84rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .fake-input span {
        color: var(--muted);
      }
      .fake-input strong {
        font-weight: 500;
      }
      .toggle-row {
        display: flex;
        gap: 0.6rem;
        margin-top: 0.6rem;
      }
      .toggle-chip {
        font-size: 0.76rem;
        border-radius: 999px;
        padding: 0.3rem 0.7rem;
        border: 1px solid rgba(148, 163, 184, 0.4);
        color: var(--muted);
      }
      .toggle-chip.active {
        border-color: var(--accent);
        background: rgba(34, 197, 94, 0.1);
        color: #bbf7d0;
      }
      .features {
        display: grid;
        gap: 1rem;
        margin-top: 3rem;
      }
      @media (min-width: 800px) {
        .features {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }
      }
      .feature-card {
        border-radius: 1rem;
        border: 1px solid rgba(148, 163, 184, 0.25);
        background: radial-gradient(circle at top left, #0f172a, #020617);
        padding: 1.2rem 1.3rem 1.1rem;
      }
      .feature-card h2 {
        font-size: 1.1rem;
        margin-bottom: 0.4rem;
      }
      .feature-card ul {
        padding-left: 1.1rem;
        margin: 0.3rem 0 0.5rem;
        font-size: 0.86rem;
        color: var(--muted);
      }
      .feature-card li + li {
        margin-top: 0.25rem;
      }
      footer {
        margin-top: 2.5rem;
        font-size: 0.75rem;
        color: var(--muted);
        border-top: 1px solid rgba(30, 64, 175, 0.3);
        padding-top: 1rem;
      }
      footer strong {
        font-weight: 600;
      }
    </style>
  </head>
  <body>
    <div class="shell">
      <header>
        <div class="logo">
          <div class="logo-mark">A</div>
          <span>Amicbridge</span>
        </div>
        <nav>
          <a href="#how">How it works</a>
          <a href="#safety">Safety</a>
          <a class="button" href="#start">Get started</a>
        </nav>
      </header>

      <main>
        <section class="hero" id="start">
          <div>
            <h1>
              Borrow from people. <span>Lend to people.</span><br />
              One simple bridge.
            </h1>
            <p class="hero-text">
              Amicbridge connects people who want to
              <strong>borrow</strong> with people who want to
              <strong>lend</strong>. You agree the terms – we just help you
              find each other.
            </p>

            <div class="cta-row">
              <button class="btn btn-primary">
                I want to borrow
              </button>
              <button class="btn btn-outline">
                I want to lend
              </button>
            </div>
            <p class="tiny-note">
              Accounts, messaging and full matching are coming soon. For now,
              you can join the early access list.
            </p>
          </div>

          <aside>
            <div class="card">
              <div class="pill">
                <span class="pill-dot"></span>
                Live preview · Example match
              </div>
              <p class="inline-label">Borrower</p>
              <div class="fake-input">
                <span>Amount</span>
                <strong>€2,000</strong>
              </div>
              <div class="input-row">
                <div class="fake-input">
                  <span>Duration</span>
                  <strong>6 months</strong>
                </div>
                <div class="fake-input">
                  <span>Max interest</span>
                  <strong>5%</strong>
                </div>
              </div>

              <p class="inline-label">Lender</p>
              <div class="fake-input">
                <span>Offer</span>
                <strong>€2,000 · 4.5% APR</strong>
              </div>

              <div class="toggle-row">
                <span class="toggle-chip active">Personal loan</span>
                <span class="toggle-chip">Small business</span>
              </div>

              <p class="tiny-note" style="margin-top: 0.8rem;">
                Example only. On the real platform you’ll be able to post your
                own request or lending offer and talk directly to matches.
              </p>
            </div>
          </aside>
        </section>

        <section class="features" id="how">
          <div class="feature-card">
            <h2>For borrowers</h2>
            <ul>
              {featuresBorrower.map((item) => <li>{item}</li>)}
            </ul>
          </div>
          <div class="feature-card">
            <h2>For lenders</h2>
            <ul>
              {featuresLender.map((item) => <li>{item}</li>)}
            </ul>
          </div>
        </section>

        <footer id="safety">
          <p>
            <strong>Important:</strong> Amicbridge is a connection platform. We
            do not provide loans, hold money, or guarantee repayments. Always do
            your own checks and consult a professional if needed before lending
            or borrowing.
          </p>
        </footer>
      </main>
    </div>
  </body>
</html>
