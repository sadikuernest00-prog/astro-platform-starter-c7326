---
const borrowerPoints = [
  "Reach real people willing to lend.",
  "Explain your situation in your own words.",
];

const lenderPoints = [
  "Set your own interest and conditions.",
  "Browse real borrowers, not anonymous forms.",
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Amicbridge – Borrow & Lend Directly</title>
    <meta
      name="description"
      content="Amicbridge connects people who want to borrow with people who want to lend."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      :root {
        color-scheme: dark;
        --accent: #22c55e;
      }
      body {
        margin: 0;
        background: #0f172a;
        color: #e2e8f0;
        font-family: system-ui, sans-serif;
      }
      .wrap {
        max-width: 900px;
        margin: 0 auto;
        padding: 2rem;
      }
      h1 {
        font-size: 2.7rem;
        margin-bottom: 0.8rem;
      }
      h1 span {
        color: var(--accent);
      }
      p {
        color: #94a3b8;
        max-width: 35rem;
        margin-bottom: 1.2rem;
      }
      .buttons {
        display: flex;
        gap: 1rem;
        margin-bottom: 2rem;
      }
      button {
        padding: 0.6rem 1.3rem;
        border-radius: 999px;
        border: none;
        font-size: 1rem;
        cursor: pointer;
      }
      .primary {
        background: var(--accent);
        color: #051b12;
      }
      .outline {
        background: transparent;
        border: 1px solid #334155;
        color: #e2e8f0;
      }
      .section {
        display: grid;
        gap: 1rem;
        margin-top: 2rem;
      }
      @media (min-width: 700px) {
        .section {
          grid-template-columns: 1fr 1fr;
        }
      }
      .card {
        background: #1e293b;
        border: 1px solid #334155;
        border-radius: 1rem;
        padding: 1rem 1.2rem;
      }
      footer {
        margin-top: 2rem;
        font-size: 0.8rem;
        color: #64748b;
      }
    </style>
  </head>
  <body>
    <div class="wrap">
      <h1>
        Borrow from people.<br />
        <span>Lend to people.</span>
      </h1>

      <p>
        Amicbridge helps borrowers and lenders find each other. You decide the
        terms — we simply make the connection.
      </p>

      <div class="buttons">
        <button class="primary">I want to borrow</button>
        <button class="outline">I want to lend</button>
      </div>

      <div class="section">
        <div class="card">
          <h2>Borrowers</h2>
          <ul>
            {borrowerPoints.map((p) => <li>{p}</li>)}
          </ul>
        </div>
        <div class="card">
          <h2>Lenders</h2>
          <ul>
            {lenderPoints.map((p) => <li>{p}</li>)}
          </ul>
        </div>
      </div>

      <footer>
        Amicbridge does not provide loans, hold money, or guarantee repayment.
      </footer>
    </div>
  </body>
</html>
