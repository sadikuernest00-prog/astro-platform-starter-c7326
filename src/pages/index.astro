
---
const featuresBorrower = [
  "Reach real people willing to lend, not just banks.",
  "Borrow in normal money, crypto, or stablecoins, depending on what you and the lender agree.",
  "Use your own wallets or bank to move funds directly.",
  "Explain your situation in your own words.",
  "Negotiate repayment terms directly with lenders.",
  "Whenever possible, we match you with manually verified lenders.",
];

const featuresLender = [
  "See real people and projects, not just numbers.",
  "Lend using bank transfer, crypto, or stablecoins – you decide.",
  "Use your own wallets or exchange accounts to send and receive crypto or stablecoins.",
  "Set your own interest rate and conditions.",
  "Chat with borrowers before you commit.",
  "Apply for a Verified lender badge after a quick manual review.",
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Amicbridge – Borrow or Lend Money, Crypto & Stablecoins</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      :root {
        color-scheme: dark;
        --bg: #020617;
        --card: #0f172a;
        --accent: #22c55e;
        --text: #e5e7eb;
        --muted: #9ca3af;
        --border: #1f2937;
      }
      body {
        margin: 0;
        background: radial-gradient(circle at top, #0f172a 0, #020617 55%);
        color: var(--text);
        font-family: system-ui, sans-serif;
      }
      .shell {
        max-width: 1000px;
        margin: 0 auto;
        padding: 2rem 1.5rem 3rem;
      }
      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      nav a {
        margin-left: 1.2rem;
        color: var(--muted);
        text-decoration: none;
      }
      nav a.button {
        background: var(--accent);
        padding: 0.45rem 0.95rem;
        border-radius: 999px;
        color: #022c22;
        font-weight: 600;
      }
      h1 {
        font-size: 3rem;
        margin-bottom: 1rem;
        line-height: 1.1;
      }
      h1 span {
        color: var(--accent);
      }
      .hero p {
        color: var(--muted);
        max-width: 36rem;
      }
      .hero-btns {
        margin-top: 1rem;
        display: flex;
        gap: 0.8rem;
      }
      .hero-btns button {
        padding: 0.7rem 1.4rem;
        border-radius: 999px;
        font-size: 1rem;
        border: 1px solid var(--border);
        cursor: pointer;
        background: transparent;
        color: var(--text);
      }
      .hero-btns .primary {
        background: var(--accent);
        color: #022c22;
        border: none;
      }
      .form-card {
        background: rgba(15, 23, 42, 0.9);
        padding: 1.5rem;
        margin-top: 2rem;
        border-radius: 1rem;
        border: 1px solid rgba(255,255,255,0.08);
      }
      .form-card h2 {
        margin-top: 0;
      }
      input, textarea, select {
        width: 100%;
        margin-top: .4rem;
        margin-bottom: 1rem;
        padding: .7rem;
        border-radius: .6rem;
        background: #020617;
        border: 1px solid #1e293b;
        color: white;
        font-size: .95rem;
      }
      textarea {
        min-height: 130px;
      }
      .submit-btn {
        background: var(--accent);
        color: #022c22;
        padding: .8rem 1.6rem;
        border-radius: 999px;
        cursor: pointer;
        font-weight: 600;
        border: none;
      }
      ul {
        padding-left: 1.2rem;
      }
      ul li {
        margin-bottom: .3rem;
        color: var(--muted);
        font-size: 0.95rem;
      }
      footer {
        margin-top: 3rem;
        padding-top: 1.5rem;
        border-top: 1px solid rgba(255,255,255,0.1);
        color: var(--muted);
        font-size: 0.85rem;
      }
    </style>

    <script>
      function showForm(type) {
        document.getElementById("borrow-form").style.display = type === "borrow" ? "block" : "none";
        document.getElementById("lend-form").style.display = type === "lend" ? "block" : "none";
        window.location.hash = type === "borrow" ? "#borrow-form" : "#lend-form";
      }
    </script>
  </head>

  <body>
    <div class="shell">

      <header>
        <h2>Amicbridge</h2>
        <nav>
          <a href="#how">How it works</a>
          <a href="#contact">Contact</a>
          <a href="#borrow-form" class="button">Get started</a>
        </nav>
      </header>

      <section class="hero">
        <h1>
          Borrow from people.<br />
          <span>Lend to people.</span><br />
          One simple bridge.
        </h1>
        <p>
          Amicbridge connects borrowers and lenders using normal money, crypto, or stablecoins.
          You set the terms — we help you match.
        </p>

        <div class="hero-btns">
          <button class="primary" onclick="showForm('borrow')">I want to borrow</button>
          <button onclick="showForm('lend')">I want to lend</button>
        </div>
      </section>

      <!-- Borrow Form -->
      <section id="borrow-form" class="form-card" style="display:none;">
        <h2>Borrow money, crypto, or stablecoins</h2>
        <p style="color:var(--muted); font-size:0.9rem;">
          Fill in your details and we’ll receive your request. Payments always happen directly
          between you and the lender (bank, crypto, or stablecoins).
        </p>
        <form name="borrow" method="POST" netlify>
          <input type="text" name="name" placeholder="Your name" required />
          <input type="email" name="email" placeholder="Email" required />
          <input type="text" name="phone" placeholder="Phone number" required />

          <select name="currency" required>
            <option value="">What do you want to borrow in?</option>
            <option value="normal">Normal money (bank transfer)</option>
            <option value="crypto">Crypto (e.g. BTC, ETH)</option>
            <option value="stablecoin">Stablecoin (e.g. USDT, USDC)</option>
          </select>

          <input type="number" name="amount" placeholder="Amount you want to borrow" required />

          <textarea
            name="message"
            placeholder="Tell us a bit about why you need this loan and any terms you prefer."
          ></textarea>

          <button class="submit-btn" type="submit">Send request</button>
        </form>
      </section>

      <!-- Lend Form -->
      <section id="lend-form" class="form-card" style="display:none;">
        <h2>Lend money, crypto, or stablecoins</h2>
        <p style="color:var(--muted); font-size:0.9rem;">
          Tell us how much you want to lend, in what form (bank, crypto, stablecoins), and on which terms.
        </p>
        <form name="lend" method="POST" netlify>
          <input type="text" name="name" placeholder="Your name" required />
          <input type="email" name="email" placeholder="Email" required />
          <input type="text" name="phone" placeholder="Phone number" required />

          <select name="currency" required>
            <option value="">What do you want to lend in?</option>
            <option value="normal">Normal money (bank transfer)</option>
            <option value="crypto">Crypto (e.g. BTC, ETH)</option>
            <option value="stablecoin">Stablecoin (e.g. USDT, USDC)</option>
          </select>

          <input type="number" name="amount" placeholder="Amount you want to lend" required />

          <textarea
            name="message"
            placeholder="Describe your lending terms (interest, duration, conditions, etc.)."
          ></textarea>

          <button class="submit-btn" type="submit">Publish lending offer</button>
        </form>
      </section>

      <!-- Features Section -->
      <section id="how" class="form-card">
        <h2>How it works</h2>

        <h3>For borrowers</h3>
        <ul>
          {featuresBorrower.map(item => <li>{item}</li>)}
        </ul>

        <h3>For lenders</h3>
        <ul>
          {featuresLender.map(item => <li>{item}</li>)}
        </ul>
      </section>

      <!-- Contact -->
      <section id="contact" class="form-card">
        <h2>Contact</h2>
        <p><strong>CEO:</strong> Robert</p>
        <p><strong>Address:</strong> Oslo, Norway</p>
        <p><strong>Phone:</strong> 929 28 373</p>
        <p><strong>Email:</strong> robertamicolsen@gmail.com</p>
      </section>

      <footer>
        <p>
          <strong>Important:</strong> Amicbridge is a connection platform.
          We do not provide loans, hold money, or guarantee repayments.
          All transfers (bank, crypto, or stablecoins) happen directly between borrower and lender.
          Always verify who you are dealing with before sending any funds.
        </p>
      </footer>

    </div>
  </body>
</html>
